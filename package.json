{
  "name": "spike-visly",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "build:shared": "yarn workspace @spike-visly/shared build",
    "dev": "run-p --print-label dev:client dev:server",
    "dev:client": "yarn workspace @spike-visly/client dev",
    "dev:server": "yarn workspace @spike-visly/server dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
